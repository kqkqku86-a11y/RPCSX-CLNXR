<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- All-files access: the native core plays games (folder dumps and ISOs) directly from
         a user-chosen storage location by raw filesystem path. Scoped storage otherwise
         blocks the core (and the Kotlin scan) from reading anything outside the app's own
         dirs. MANAGE_EXTERNAL_STORAGE is API 30+; on API 29 requestLegacyExternalStorage +
         READ_EXTERNAL_STORAGE gives the same raw-path access. Granted by the user in system
         settings, only when they set a Games folder. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" />
    <uses-feature android:name="android.hardware.usb.accessory" />

    <application
        android:name=".RpcsxApplication"
        android:allowBackup="true"
        android:allowNativeHeapPointerTagging="false"
        android:requestLegacyExternalStorage="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_rpcsx"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_rpcsx_round"
        android:supportsRtl="true"
        android:appCategory="game"
        android:theme="@style/Theme.RPCSX"
        tools:targetApi="31">

        <service
            android:name=".PrecompilerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
        </service>

        <meta-data 
            android:name="android.game_mode_config"
            android:resource="@xml/game_config" />
        
        <activity
            android:name=".RPCSXActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="sensorLandscape"
            android:launchMode="singleTask"
            android:exported="true" >
            <!-- Internal launch from the library UI (explicit class + "path" extra). -->
            <intent-filter>
                <action android:name="rpcsx.intent.action.Emulator" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- External launch by Android emulation frontends (Daijisho, ES-DE, Pegasus).
                 Frontends that pass the game in a string extra reach us by explicit component
                 (exported=true) and need no filter; this VIEW filter additionally handles
                 ACTION_VIEW launches and file-manager "Open with", scoped to PS3 disc/package
                 file extensions so we never appear as a handler for unrelated files. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="*/*" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.iso" />
                <data android:pathPattern=".*\\.ISO" />
                <data android:pathPattern=".*\\.pkg" />
                <data android:pathPattern=".*\\.PKG" />
            </intent-filter>
        </activity>

        <activity
            android:name=".overlay.OverlayEditActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="sensorLandscape"
            android:launchMode="singleTask"
            android:exported="false" >
        </activity>
        
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <provider
            android:authorities="${applicationId}.documents"
            android:name="net.rpcsx.provider.AppDataDocumentProvider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <receiver
            android:name=".utils.PackageInstallStatusReceiver"
            android:exported="false" />
    </application>
</manifest>
